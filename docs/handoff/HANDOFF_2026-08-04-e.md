# HANDOFF — 04/08/2026 · Sessão 1, parte 5

> **Cadeia:** [`ESTADO.md`](../../ESTADO.md) → **este handoff** → [`BACKLOG.md`](../../BACKLOG.md)
> **Roteador:** [`AGENTS.md`](../../AGENTS.md) · **Índice:** [`INDICE.md`](INDICE.md) ·
> **Anterior:** [`HANDOFF_2026-08-04-d.md`](HANDOFF_2026-08-04-d.md)
> **Solicitações:** [`../solicitacoes/INDICE_DE_SOLICITACOES.md`](../solicitacoes/INDICE_DE_SOLICITACOES.md) ·
> **Histórico:** [`../historico/INDICE.md`](../historico/INDICE.md) ·
> **Fontes:** [`../INVENTARIO_DE_FONTES.md`](../INVENTARIO_DE_FONTES.md)

---

## A escolha desta parte: a afirmação mais consequente que ninguém tinha conferido

O backlog técnico estava vazio pela segunda vez. Em vez de procurar tarefa, a ordem foi outra:
**listar o que o projeto afirma e perguntar qual dessas afirmações nunca foi testada de verdade.**

A resposta foi desconfortável. A promessa central do Flavio é literal:

> *"você não vai apagar o passado! O passado continua."*

O histórico congelado (01/03 → 04/08/2026, 66 dias) foi construído **rodando o gerador do site
antigo a partir do código-fonte**. Isso prova fidelidade ao **código**. Não prova fidelidade ao que
os irmãos **viram na tela** — e é a tela que eles têm na memória.

Entre o código e a tela existem o *bundle* publicado (que pode estar defasado em relação ao `src/`),
a ordem da renderização, o fuso do navegador e qualquer transformação que a interface faça. Cada um
desses pode deslocar um nome. Se o site novo mostrar um março diferente do que foi divulgado, ele
**desmente** o que as pessoas viram — pior do que não ter congelado nada.

## O que foi feito

`scripts/conferir-historico-contra-site-antigo.mjs` abre o **site antigo** num navegador de
verdade, consulta os 66 dias um a um e compara nome a nome contra o `blocos.json` publicado.

```
  dias conferidos ......... 66 de 66
  dias que não apareceram . 0
  nomes conferidos ........ 282
  divergências ............ 0
```

**As 282 escalações do passado batem com o que o site antigo mostra na tela.** A promessa se
sustenta — agora medida, não presumida.

## 🔴 O achado que apareceu de lado: o site antigo não mostra o passado

A primeira versão da varredura rolava a página até o fim e lia tudo. O portão a **reprovou**:

```
🔴 O site antigo só renderizou 0 dos 66 dias
```

Zero. E não era defeito do script — é o comportamento do site: **ele lista do dia de hoje em
diante**, com corte físico para a imagem do WhatsApp não ficar gigante.

Consequência real, hoje: **um irmão que abra o link antigo não consegue ver março a julho.** Só
digitando a data na busca. O site novo mostra, porque o passado ali é **dado**, não recálculo.

Isso reforça — sem substituir — o item P1.1: o link antigo já é pior que o novo em duas frentes,
não só na Santa Ceia.

## 🔴 E o erro que o portão pegou em mim: régua errada lida como divergência total

Reescrita a varredura para usar a busca, o resultado foi **de novo 0 de 66**. Mas a versão por
amostra, usando **a mesma busca**, tinha achado os nomes. Logo o defeito estava no guard, não na
varredura.

O guard procurava a data em `01/03`. A tentação era ajustar o formato no chute. Em vez disso,
**mediu-se o que o site realmente escreve**:

```
MAR
01
DOMINGO
```

Mês abreviado e dia em **linhas separadas** — `01/03` não existe em lugar nenhum daquela tela.

Duas lições, e a segunda é a que importa:

1. **Formato se mede, não se supõe.** Foram duas tentativas erradas antes de abrir o navegador e
   ler o texto renderizado.
2. **"0 de 66" se lê como divergência catastrófica quando é só régua errada.** Um número vermelho
   dramático convence tão bem quanto um verde falso — e leva à conclusão oposta ao fato.

O guard existia justamente para isso: sem ele, `nomes faltando = 0` num texto vazio teria produzido
**"✅ 66 dias conferidos"** sem ter conferido nada. Ele foi o que impediu o verde vazio, e por isso o
comentário do código agora carrega o formato medido, com o porquê.

## Decisão declarada: esta conferência **não** entra no GATE

O GATE tem de rodar sempre e sem depender de terceiros. Esta checagem depende do **GitHub Pages do
repositório antigo** estar no ar e continuar respondendo igual — e o Flavio decidiu que aquele
repositório **não é tocado**, mas nada garante que ele siga existindo.

Portão que quebra por causa alheia é portão que alguém desliga. Então:

- **Fora do GATE**, rodado sob demanda: `node scripts/conferir-historico-contra-site-antigo.mjs`
- **O resultado fica registrado aqui**, com data — 04/08/2026, 66/66, 282 nomes, 0 divergências.
- **Quando refazer:** se o bloco histórico for tocado por qualquer motivo. Ele é imutável por
  desenho, então em condições normais isso não acontece.

## E o que o pré-voo deste projeto encontrou no MÉTODO

Rodando o portão de tamanho de documentos aqui, ele imprimiu **"15 arquivo(s) medido(s)"** — e este
projeto tem **16**. O 16º é `docs/historico/INDICE.md`, isento por estar sob `historico/`.

Só que o índice do histórico é o **único documento vivo** daquela pasta: cresce a cada rotação. O
arquivo com maior chance de estourar o teto era o único que ninguém media — e o relatório não dava
sinal disso.

Puxando o fio, apareceram, em `D:\Antigravity\_padroes-globais`:

| O que | Estado |
|---|---|
| Portão de tamanho: 3 defeitos (índice isento · fatia pulada em silêncio · isentos nunca impressos) | corrigido, autoteste 14/14, provado contra a versão anterior (10/14) |
| **ERRO 15 e ERRO 27 existiam duas vezes**, com conteúdos diferentes | desfeito — moveu-se quem **não** era citado |
| Índice de erros parado no 26 enquanto existiam 31 | reconstruído: 35 erros, 0 lacunas |
| `ANTEPAROS.md` com 533 linhas (teto 400) | dividido por assunto: 148 linhas + 3 fatias |
| **12 scripts do método e UM autoteste, que ninguém rodava** | o pré-voo ganhou o grupo `MÉTODO`, que roda todos |

Registrado como **ERRO 32** e **ERRO 35** no catálogo. Detalhe no commit `b08b6ec` de
`flaviocom/padroes-globais`.

🔴 **Um erro meu no meio disso, que vale mais que os achados:** escrevi uma checagem exigindo que o
título do índice fosse igual ao da seção. Ela acusou 7 divergências — e as 7 eram resumos **curados**,
mais ricos que o título. Pior: eu já tinha reconstruído o índice por script para satisfazê-la,
**apagando 26 resumos escritos à mão**. Recuperados da cópia feita minutos antes. A checagem foi
removida. *Nunca conserte em massa o que um portão novo acusou — o primeiro suspeito é o portão.*

## A ponta que faltava: o site NOVO contra o dado publicado

Conferir o passado contra o site antigo cobria metade. A outra metade nunca tinha sido medida:
**o que foi publicado como dado chega inteiro à tela do site novo?**

A pergunta não é retórica. Entre o `blocos.json` e o que a pessoa vê existem o filtro, a ordenação,
o agrupamento por mês, o corte de exibição e o fuso do navegador — e a conferência do site antigo
acabou de mostrar que uma suposição sobre renderização pode estar completamente errada (ele **não
mostra o passado**, o que ninguém imaginava).

`scripts/conferir-site-contra-dados.mjs`, agora `npm run vivo:conferir`:

```
  dias conferidos ......... 131 de 131
  dias que não apareceram . 0
  nomes conferidos ........ 543
  divergências ............ 0
```

**131 dias, 543 escalações, nenhuma perdida entre o dado e a tela.** E a Santa Ceia foi conferida
pelos dois lados: o dia aparece, o aviso aparece, e **nenhum dos 16 irmãos** está na tela.

Os dois únicos dias sem gente são exatamente os dois de Santa Ceia — **07/06** preservado no bloco
histórico (é o que os irmãos viram no site antigo; passado não se reescreve) e **16/08** no bloco
novo, na data certa. Nenhum dia vazio inexplicado.

### E as validações ao vivo ganharam nome

Elas só existiam para quem vasculhasse `scripts/`. Uma IA nova lendo o `package.json` veria o GATE e
concluiria que não há validação de produção. Agora: `vivo`, `vivo:admin`, `vivo:celular`,
`vivo:conferir`, `vivo:conferir-passado` — com um comentário no próprio manifesto dizendo **por que
não entram no GATE** (dependem da rede; portão que quebra por causa alheia é portão que alguém
desliga).

## O ensaio do cenário que deu origem ao projeto

A tela de trocar elenco existe e foi validada ao vivo. Mas **validar a tela não é validar o
cenário**: a tela renderiza, os botões respondem, a validação mostra 15 regras. Nada disso responde
à pergunta do Flavio — *quando alguém sai e alguém entra com restrições novas, a escala se refaz
inteira, válida, e o passado fica intocado?*

`npm run ensaio` roda isso de ponta a ponta, sem rede e sem credencial, com o **mesmo código do
produto** (compilado na hora, sem cópia paralela). Sai o mais escalado (Flavio, 17 turnos); entra um
irmão com **as quatro famílias de restrição ao mesmo tempo** — só dom e qua, nunca sábado, só noite,
teto de 2/mês, e novembro inteiro fora.

```
  ✅ Flavio saiu: zero escalas depois do corte      0 turnos
  ✅ o irmão novo foi escalado                      4 turnos
  ✅ família 1 — dia da semana (só dom e qua)       nenhum fora
  ✅ família 2 — turno (só noite)                   nenhum fora
  ✅ família 3 — teto mensal (máx. 2)               2026-10:2 · 2026-12:2
  ✅ família 4 — ausência (novembro inteiro)        nenhum turno em novembro
  ✅ as 15 regras do catálogo                       0 falha(s) dura(s) · 3 aviso(s)
  ✅ o passado ficou intocado (byte a byte)         128 turnos antes do corte, idênticos
  ✅ o distanciamento não desabou                   menor intervalo: 6 dia(s)
  ✅ equilíbrio entre os 12 sem restrição           11 a 12 turnos — amplitude 1
  ✅ quem tem teto é aproveitado                    Thiago 6/6 · Williams 9/9 · Irmão 4/4
```

**11 de 11.** O pedido original, medido.

### 🔴 A régua que reprovava o que não estava quebrado

A primeira versão media a **amplitude bruta** da carga e reprovava: *"6 a 12 turnos"*. Não era
defeito. O Thiago tem teto de 2/mês, então o máximo dele em três meses **é 6** — ele estava no
limite, não sub-escalado. **Comparar a carga de quem tem teto com a de quem não tem é comparar
coisas incomparáveis**, e produz um vermelho que faz consertar o que está certo.

Trocada por duas perguntas separadas, que dizem mais: *entre quem tem a mesma disponibilidade, a
carga é parecida?* (amplitude **1**) e *quem tem teto está sendo aproveitado?* (**todos em 100%** da
cota). Nenhuma das duas apareceria na régua ingênua.

### 🔴 O motivo que ficava mudo — e as duas rodadas que ele me custou

O ensaio começou com o irmão novo pegando **zero turnos**, e a tela do gerador dizia apenas
**"só pode "** — a frase terminava ali. Era `ROTULO_TURNO['noite']` (minúsculo, valor inválido; o
literal é `'NOITE'`) devolvendo `undefined`, e `join` transformando `undefined` em string vazia.

O defeito de origem era do ensaio, que usava `t.turno` em vez de `t.tipo` e o literal errado. Mas o
**motivo mudo é do produto**, e mandou procurar defeito no gerador. Corrigido com um fallback e
**4 testes**: sem a correção, 3 deles falham; o quarto — rótulo válido — passa nas duas versões de
propósito, que é a ponta limpa do par.

### 🔴 E o erro mais instrutivo: inferi que uma edição tinha acontecido

Escrevi os testes num comando que também tinha um `| tail`. O **hook do pipe rejeitou o comando
composto inteiro** — inclusive o `cat >>` que teria funcionado. Rodei os testes de novo, vi **43
passando**, e li isso como *"40 anteriores + os meus 3"*.

Eu nunca tive a linha de base. **43 já era o número de antes.** Passei três medições provando que
"os testes passam com o código antigo" — e os testes não existiam. Só o `git status` desmentiu.

## Estado ao fechar

| | |
|---|---|
| GATE | 8 passos, verde |
| Órfãos | 16 documentos, 0 órfãos, 0 ligações quebradas |
| Histórico congelado | **conferido contra a tela do site antigo** — 66/66 dias, 282 nomes |
| Pendente | 👤 as duas credenciais · 👤 auditor independente |
