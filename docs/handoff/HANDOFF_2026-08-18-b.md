# HANDOFF — 18/08/2026 (parte 2) · a árvore de capacidades chegou

> **Cadeia de navegação:** [`ESTADO.md`](../../ESTADO.md) → **você está aqui** → [`BACKLOG.md`](../../BACKLOG.md)
> **Roteador:** [`AGENTS.md`](../../AGENTS.md) · **Solicitação:** [S-058](../solicitacoes/INDICE_DE_SOLICITACOES.md) ·
> **Por quê:** [DB-054](../../DIARIO_DE_BORDO.md) · **Handoff anterior:** [18/08 parte 1](HANDOFF_2026-08-18.md)

## 0. Para quem chega agora — em uma frase

O Flavio apontou para um método global novo, 100% não commitado em
`D:\Antigravity\_padroes-globais\` (árvore de capacidades + coexistência de harnesses), e pediu
para eu **adaptar à realidade deste projeto** sem tocar no padrão-ouro em si — mudança nesse
diretório é decisão dele. O escala-porteiros ganhou `docs/capacidades.json` (60 scripts
catalogados) e a seção `## Handoff ativo` em `ESTADO.md`, e o processo achou um checador de
citações melhor que estava órfão.

## 1. O que já existia em `_padroes-globais`, sem eu ter escrito nada

Duas peças novas, cada uma com pesquisa própria registrada e portão que morde:

| Peça | O que resolve | Pesquisa | Portão |
|---|---|---|---|
| `ARVORE_DE_CAPACIDADES.md` | skill/agente/ferramenta/hook local vira "folha" catalogada — nada fica órfão | `pesquisas/2026-08-18_capilaridade-e-portoes.md` | `checar-capilaridade.mjs` |
| `COEXISTENCIA_HANDOFF_E_CONTEXTO.md` | Claude Code, Codex e DeepSeek no mesmo projeto sem duas memórias | `pesquisas/2026-08-18_portabilidade-handoff-contexto.md` | `checar-handoff.mjs` |

Os dois autotestes (`autoteste-checar-capilaridade.mjs`, `autoteste-checar-handoff.mjs`) passaram
quando rodados. Não commitei nada em `_padroes-globais` — está exatamente como estava.

## 2. O que entrou no escala-porteiros

1. **`docs/capacidades.json`** — os 60 scripts de `scripts/` viraram folhas, agrupados em 4 ramos
   (`metodo` → `PORTOES.md`, `validacao-ao-vivo` → `OPERACAO.md`, `dados-e-algoritmo` →
   `ALGORITMO.md`, `comunicacao` → `LEMBRETE_WHATSAPP.md`). `AGENTS.md` ganhou a §6.1 apontando
   para o mapa (exigido pelo portão: `AGENTS.md não aponta para capacidades.json` seria erro).
2. **`## Handoff ativo`** em `ESTADO.md`, com "Sem handoff ativo." — formato pronto para quando
   houver troca de agente ou trabalho paralelo.
3. **Verificado nas duas pontas contra o projeto real:** `checar-capilaridade.mjs .` → OK;
   `checar-handoff.mjs .` → exit 0 (a mensagem impressa é enganosa — ver achado abaixo);
   `pre-voo.mjs .` (global) → grupo MÉTODO mostra "árvore de capacidades ✅".

## 3. Achados no caminho

🔴 **`scripts/conferir-citacoes.mjs` é mais rigoroso que `scripts/portao-citacoes.mjs`, e é o
órfão.** O primeiro (origem do P6.10, 05/08) confere o SÍMBOLO citado junto da linha; o segundo
(ligado a `npm run citacoes`, no gate) só confere que arquivo e linha existem. Nenhum `npm run`
aponta para o mais rigoroso. Virou **P8.1** no `BACKLOG.md` — decisão do dono entre religar ou
fundir, não resolvido sozinho.

⚠️ **`scripts/provar-conferencia-por-turno.mjs` não é reexecutável com o mesmo sentido hoje.**
Referencia um caminho de build que não existe mais, mas o problema é mais fundo: ele compara contra
`git show HEAD:...`, e o HEAD de hoje já contém a correção que a prova demonstrava — rodar agora
reprovaria dos dois lados. Corrigida só a descrição no mapa (não afirmar reprodutibilidade que não
existe); o script fica como registro histórico, como um handoff.

⚠️ **`checar-handoff.mjs` (global) tem mensagem enganosa, não corrigida** (não é para alterar o
padrão-ouro): "seção ausente" e "seção presente dizendo N/A" imprimem a MESMA linha de log —
`resultado(true, false)` nos dois lugares do script. O código de saída está certo (0); só a
mensagem confunde. Fica para o Flavio decidir se pede o ajuste.

## 4. Estado final

`npm run gate` do escala-porteiros: **33 passos, `EXIT_GATE=0`, selo `eb2cc89056dc`**.

## 5. Como reverter

`git revert` do commit desta entrada — `docs/capacidades.json`, a §6.1 do `AGENTS.md` e a seção
"Handoff ativo" do `ESTADO.md` somem; P8.1 sai do `BACKLOG.md`. Produto: nada muda. Nada em
`_padroes-globais` foi commitado por esta sessão, então não há o que reverter lá.
