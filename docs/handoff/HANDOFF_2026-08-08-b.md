# HANDOFF — 08/08/2026 (parte 2) · o loop autônomo: merge na main, e a fila medida antes de mexida

> **Cadeia de navegação:** [`ESTADO.md`](../../ESTADO.md) → **você está aqui** → [`BACKLOG.md`](../../BACKLOG.md)
> **Roteador:** [`AGENTS.md`](../../AGENTS.md) · **Solicitações:** [S-054 a S-056](../solicitacoes/INDICE_DE_SOLICITACOES.md) ·
> **Por quês:** [DB-050 a DB-052](../../DIARIO_DE_BORDO.md) · **Handoff anterior:** [08/08 parte 1](HANDOFF_2026-08-08.md)

## 0. Para quem chega agora — a tarde em uma frase

A informação do dono (*"o site já foi divulgado, os irmãos já usam"*) zerou P0 e P1 pelo registro
alcançar a realidade; o trabalho do dia (S-052 a S-055) foi **mergeado na `main` por rebase** com a
prova de que o site servido não muda um byte; e o loop do "go" fechou a fila P5 — onde **dois dos
três itens já estavam prontos no código** e só o registro devia.

## 1. S-054 e S-055 — os fechamentos por realidade

- **P0.1** aprovado pelo fato; **P0.2** metade paga (o `GITHUB_PAT` está no navegador dele desde
  06/08 — provado pelos commits via API; resta a chave **opcional** do motor).
- **P1.1, P1.2, P1.3** fechados pela divulgação (risco residual do link antigo declarado no P1.1).
- **Merge por rebase** (`main` linear, `35d7519` → depois `1392e62`), sem re-rodar NO AR — medido
  que o diff não toca produto nem dado publicado; o veredito de 07/08 segue sendo a medição válida.

## 2. S-056 — o loop, e o que o mapeamento achou antes de qualquer edição

**Regra do go: mapear as ligações antes de mexer.** O mapeamento mudou o plano duas vezes:

| Item | O que o mapa mostrou | O que foi feito |
|---|---|---|
| **P5.4** (validação de esquema) | **JÁ ESTAVA PRONTO** — `conferirEsquema` no `carregar.ts` desde 06/08 (`b3ca016`), ligado no carregamento, com `esquema.test.ts` nas duas pontas. Terceira ocorrência da classe "aberto no papel, pronto no código" | Registro fechado com a prova datada |
| **P5.7** (casos-limite sem saída esperada) | O código já fazia o certo nos três casos — **medido por sonda antes de afirmado**: recusa declarada com motivo acionável (D-03) nos dois impossíveis, geração normal no dia único com culto | Saída esperada escrita (`ALGORITMO.md` § Casos-limite) e **travada** em `casos-limite.test.ts` (4 testes) |
| **P5.3** (teto do `blocos.json`) | A rede não alcança a documentação da API — teto sem fonte seria chute | A metade local MEDIDA: 41.815 bytes ÷ 183 turnos = **228 bytes/turno, ~49 KB/ano**; o número do teto fica declarado por medir |

## 3. O que NÃO entrou, e por quê

- **P5.2** (dois administradores publicando) — a política ("relê e reaplica?") é decisão 👤.
- **P5.6** (CI) — ligar GitHub Actions consome recurso da conta dele; decisão 👤.
- **P4.9** (piso como máximo provado) e **P4.15** (tolerância configurável) — declarados por
  desenho; mudá-los altera comportamento de produto sem pedido.
- **Credenciais** — o caminho `D:\...\.credenciais.env` está no PC dele, inalcançável da nuvem; e a
  regra §9.1 do roteador ("nem com autorização") continua valendo. Nada da fila dependia disso.

## 4. Como reverter

`git revert` do commit desta entrada — o teste de casos-limite e a seção do `ALGORITMO.md` somem;
os fechamentos de registro voltam a mentir para o lado do medo. Produto: nada muda.
