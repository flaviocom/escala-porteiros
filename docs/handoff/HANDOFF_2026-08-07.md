# HANDOFF — 07/08/2026 · conferência do publicado, pesquisa mundial e a auditoria das duas réguas

> **Cadeia de navegação:** [`ESTADO.md`](../../ESTADO.md) → **você está aqui** → [`BACKLOG.md`](../../BACKLOG.md)
> **Roteador:** [`AGENTS.md`](../../AGENTS.md) · **Solicitações:** [S-040, S-041, S-042](../solicitacoes/INDICE_DE_SOLICITACOES.md) ·
> **Por quês:** [DB-042 a DB-044](../../DIARIO_DE_BORDO.md) · **Handoff anterior:** [06/08](HANDOFF_2026-08-06.md)

## 0. Para quem chega agora — o dia em uma frase

O dono publicou a escala 06/08→31/12 pela tela; foi conferida por fora (nada se perdeu nos deploys
cancelados), uma **pesquisa mundial de métodos** confirmou o desenho do motor com a recomendação
central **medida e recusada**, e a varredura de variações achou e fechou **três defeitos reais** —
o pior deles: as duas réguas cegas para a MESMA ausência invertida.

## 1. S-040 — "os deploys cancelados perderam alguma coisa?"

**Não, e está medido.** Cancelar *deploy* não cancela *commit*: cada publicação sobe a árvore
inteira do SHA dela. Dos 4 commits dele, 2 eram de escala (1 linha por pasta de dados) e **2
estavam vazios** (`pessoas.json` não muda desde 06/08 12:52 — salvar o elenco não teve o que
gravar). O site serve dado **idêntico** ao repositório — a diferença de bytes que assustou era
CRLF×LF, e comparar bytes onde a pergunta é sobre DADO produz alarme falso.

Nasceu o **auditor independente do site** (`npm run vivo:auditoria`): recontagem do zero, sem uma
linha de `src/`, sobre o dado baixado pela URL. Detalhe: [DB-042](../../DIARIO_DE_BORDO.md).

## 2. S-041 — pesquisa mundial + varredura de variações + auditoria das réguas

**Pesquisa** (delegada ao Gemini, registrada com auditoria PREPENDADA — inclusive um erro dela:
assumiu teto para os 14, e só 1 pessoa tem):
[`PESQUISA_2026-08-07-metodos-rostering.md`](../superpowers/specs/PESQUISA_2026-08-07-metodos-rostering.md).
Veredito: **manter GRASP**; CP-SAT/WASM no navegador é custo sem ganho nesta escala.

**A recomendação central (busca local pós-GRASP) foi MEDIDA e RECUSADA**: 0 trocas melhoradoras
nos dados reais — ótimo local; Jain 0,9965 é o máximo aritmético (258 vagas ∤ 14). A prova é
re-rodável e é o portão de reabertura da decisão: `npm run experimento:busca-local`.

**Três defeitos reais, cada um provado com mutante nas duas pontas:**

| Defeito | O estrago que fazia | Onde fechou |
|---|---|---|
| 🔴 Ausência INVERTIDA (fim < início) ignorada **pelas duas réguas** | pessoa escalada dentro da própria viagem, tudo verde | normalização INDEPENDENTE em `regras.ts` e `conferencia-independente.ts` |
| 🔴 Grade vazia saía `ok: true` | período invertido/sem culto = escala vazia com cara de sucesso | guarda em `gerar()` que declara a falha nomeando o motivo |
| 🔴 2ª régua sem infrator de `diasProibidos`/`turnosPermitidos` | ela podia parar de conferir os dois campos com as 28 verdes intactas | 2 infratores injetados em `conferencia-independente.test.ts` |

**Método novo** (da pesquisa §6): *property-based testing* sem dependência —
`src/dominio/variacoes.test.ts`, 150 elencos forjados por PRNG semeado, metade das ausências
invertida de propósito, contrato inteiro (gerar → 1ª régua → 2ª régua) em cada um.

## 3. S-042 — "a escala no ar está correta? pode divulgar?"

**Sim — e o veredito é medido, não de cabeça.** `npm run vivo:veredito` baixa o dado PELA URL e
roda as duas réguas, regra a regra:

| | resultado |
|---|---|
| Bloco vigente | `bloco-2026-08-06-2026-12-31` · 87 turnos (86 comuns + Santa Ceia 16/08) · 258 vagas, todas preenchidas |
| 1ª régua (catálogo) | **17 de 17 avaliadas · 0 falhas duras** · 1 aviso (Q4, variedade de companhia — qualidade, não erro) |
| 2ª régua (independente) | **8 de 8 promessas sem furo** · 258 escalações conferidas uma a uma |
| Auditor do site | **0 divergências** contra a tela pública (16 linhas × 10 meses) |
| Piso | 4 dias, declarado = real · 5 pessoas no mínimo: Donizete, Flavio, Isac, Luíz Cezar, Williams |
| Equilíbrio | 18–19 turnos, diferença 1 · Williams (teto 3/mês): 12 turnos, nenhum mês estourado |
| Passado | intacto byte a byte · Eduardo e Thiago: 0 turnos de 06/08 em diante |

## 3b. S-043 — "token no navegador é o melhor formato?" — decisão de FASE

**Fica assim na fase 1, com o desenho da fase 2 registrado.** Para um administrador, o fluxo real
(entrar → gerar → conferir → publicar → site em ~1 min) é usabilidade boa; o custo é o cadastro do
token, uma vez por navegador. Comercializar exigirá backend + conta e-mail/senha + multi-tenant —
desenho na §P4.y do `BACKLOG.md`, ponteiros no `ARQUITETURA.md` e no `github.ts` (o arquivo que
será reescrito naquela fase). O domínio não muda uma linha — o portão `arquitetura` garante.
Detalhe: [DB-045](../../DIARIO_DE_BORDO.md).

## 3c. S-045/S-046 — o modelo de comunicação, e a prova de que 2027+ é reproduzível

**[`COMUNICACOES.md`](../COMUNICACOES.md)**: a mensagem de estreia do WhatsApp (como o dono a
enviou, com as correções dele) virou o modelo da casa — estrutura bloco a bloco, banco de
versículos ARC sobre porteiros, checklist. Ligado no roteador e no inventário do `generico:docs`.

**Reprodutibilidade plurianual, medida:** 2027, 2028 e 2029 geram com o elenco real e a fronteira
de 2026 — duas gerações idênticas byte a byte, 0 falhas nas duas réguas. O furo era documental:
nasceu a seção **"A virada de ano"** no [`OPERACAO.md`](../OPERACAO.md) (Santa Ceia do ano novo
ANTES de gerar — a lacuna dos 104 domingos de 2026-08-06 —, elenco, fronteira, divulgação).
Detalhe: [DB-046](../../DIARIO_DE_BORDO.md).

## 4. Os incidentes de instrumento do dia — a lição que fica

1. **O auditor do site quebrou no dia seguinte à publicação**: gerava o período pela tela, e a tela
   recusa data retroativa. Funcionou em 06/08 por coincidência. Redesenhado: confronto público
   SEMPRE; confronto do admin pulado-e-DECLARADO quando retroativo.
2. **Leitor colado, duas vezes**: `Adilson19 turnos` e `Eduardosaiu` — o DOM cola elementos irmãos.
   A regra: casar contra o CADASTRO, nunca regex sobre texto de tela.
3. **`Date.UTC` num teste novo** — o portão de datas pegou; virou aritmética pura de calendário.
   Teste não é isento da regra da casa.

## 5. Como continuar

```bash
cd C:/Users/oflav/build/escala-porteiros
npm run gate                  # 32 passos, tem de sair 0
npm run vivo:no-ar            # as validações do site publicado (inclui veredito e auditoria)
npm run experimento:busca-local  # o portão de reabertura da decisão do motor
```
