# Diário de solicitações — 21/08/2026

> **O que é este arquivo:** registro granular, no calor da hora, de cada solicitação do Flavio —
> distinto de [`DIARIO_DE_BORDO.md`](../../../DIARIO_DE_BORDO.md) (o "porquê" mais profundo de cada
> decisão) e de [`docs/handoff/`](../../handoff/INDICE.md) (a síntese de fechamento de sessão).
> Formato e regra de rotação: skill global `/historico`, Regime B-especial de
> `_padroes-globais/ROTACAO_DE_DOCUMENTOS.md`.
>
> **Cadeia de navegação:** [`ESTADO.md`](../../../ESTADO.md) → **você está aqui** → [`BACKLOG.md`](../../../BACKLOG.md)
> **Índice:** [`INDICE.md`](INDICE.md) · **Roteador:** [`AGENTS.md`](../../../AGENTS.md)

---

### 08:17 SP — implantar o diário contínuo de solicitações

**Pedido:** o Flavio pediu, textualmente, para implantar e manter um diário contínuo de
solicitações neste projeto e em padrões globais — com regra de descoberta de convenção existente,
apresentação da última interação com evidência ao retomar, formato de entrada por horário de São
Paulo (Pedido/Feito/Status/Aprendizado/Cuidado), disciplina append-only, rotação automática a
700 linhas/90 KB, e diferenciação clara entre `ESTADO.md`/`BACKLOG.md` (vivos), diário (histórico
imutável) e handoff (síntese de fechamento). Pediu para registrar esta própria solicitação como a
primeira entrada depois de implantado.

**Feito:**
1. Descoberta de convenção (Passo 0 da skill `/historico`): este projeto já tem `AGENTS.md`,
   `ESTADO.md` e `BACKLOG.md` na raiz → convenção `docs/historico/SOLICITACOES/AAAA-MM-DD.md` +
   `docs/historico/SOLICITACOES/INDICE.md`. Ambos criados agora (este arquivo e o índice).
2. **Achado, não inventado**: o mecanismo que o Flavio descreveu já existe, formalizado
   **hoje mesmo**, em `_padroes-globais/ROTACAO_DE_DOCUMENTOS.md` §"Regime B-especial — diário
   gerado por comando", nascido de uma sessão no projeto ThetaLens V3. A skill global `/historico`
   já implementa o passo a passo exato que ele descreveu — confirmado lendo a skill antes de agir,
   em vez de reinventar por conta própria.
3. **Última interação apresentada com evidência (Passo 1)**, as três fontes concordam, sem
   divergência: `git log -1` → `a1cfb3a`, 20/08/2026 23:38, "Handoff do dia inteiro: nasceu o
   escala-geral, horário real virou regra máxima"; última entrada de
   [`DIARIO_DE_BORDO.md`](../../../DIARIO_DE_BORDO.md) → DB-069, mesmo tema; topo de
   [`ESTADO.md`](../../../ESTADO.md) → mesmo pivô, mesmo link de handoff.
4. **Defeito de infraestrutura achado e corrigido no caminho**:
   `docs/solicitacoes/INDICE_DE_SOLICITACOES.md` é, pela regra do próprio Regime B-especial, um
   documento HISTÓRICO por natureza (uma entrada por solicitação, nunca reescrita) — mas não
   estava declarado na lista `historico` de `docs/regimes-documentos.json`, então o portão
   `checar-tamanho-docs.mjs` o mediria pelo regime errado (*referência*, 800/100, dividir por
   assunto) em vez do certo (*histórico*, dividir por período). Corrigido: entrada acrescentada em
   `docs/regimes-documentos.json` (ver commit desta rodada).
5. Este arquivo cross-referenciado com `DIARIO_DE_BORDO.md` (nova entrada DB-070, ver lá) e com
   `docs/historico/INDICE.md` (o índice-mestre de fatias, que agora também aponta para esta pasta).

**Status:** ✅ feito.

**Aprendizado:** perguntar "você já está operando assim?" antes de aceitar uma especificação
detalhada é o movimento certo — a resposta era "parcialmente": a estrutura (diário raiz + rotação
por período + índice) já existia sob outro nome (`DIARIO_DE_BORDO.md`), mas o formato granular por
solicitação (HH:MM, Pedido/Feito/Status) não. As duas coisas não competem: coexistem, uma para o
registro no calor da hora, outra para o porquê mais profundo — a própria skill já previa essa
divisão de trabalho.

**Cuidado:** não confundir os dois regimes de "histórico" deste projeto — `DIARIO_DE_BORDO.md`
(Regime B clássico, raiz, teto 2.000 linhas/virada de MÊS) e este diário de solicitações (Regime
B-especial, subpasta, teto 700 linhas/90 KB, corte por SESSÃO/DIA). São tetos diferentes de
propósito, não um erro de configuração — aplicar o teto errado a qualquer um dos dois rotacionaria
cedo ou tarde demais.

### 13:39 SP — auditar, implantar e OPERAR o método compartilhado (Diário + Cofre de Conhecimento)

**Pedido:** o Flavio pediu, com um prompt extenso e numerado (Passo 0 a Passo 6), para trazer este
projeto para o método compartilhado de todos os projetos dele — não como configuração pontual, mas
como o MODO DE OPERAÇÃO permanente daqui pra frente, retroalimentando sete pontos (motores/
capacidades, documentos, visualizações, ligações, pesquisas, achados/decisões/erros, conhecimento
cross-projeto) no mesmo passo em que o trabalho acontece.

**Feito:**
1. Confirmado acesso a `D:\Antigravity\_padroes-globais\` e lidos por inteiro
   `PORTABILIDADE_ENTRE_IAS.md`, `DIARIO_DE_SOLICITACOES.md` e `COFRE_DE_CONHECIMENTO.md` antes de
   agir (Passo 0).
2. Portabilidade (Passo 1): `AGENTS.md`/`ESTADO.md`/`BACKLOG.md` já presentes e atuais — nada a
   criar.
3. Diário de Solicitações (Passo 2): já implantado horas antes (entrada das 08:17, acima). Rodado
   `checar-diario-solicitacoes.mjs` — 5/5 checagens verdes. Roteador (`AGENTS.md`) já linkava o
   índice; rótulo ajustado para "diário vivo, desde 21/08/2026 (regra global #10)", no padrão do
   `charmway-erp/CLAUDE.md`.
4. **Cofre de Conhecimento (Passo 3) — o trabalho de verdade:** primeiro nó do `escala-porteiros`
   no Cofre. Inventariado código real (não documentação): motor de escala (GRASP), validação dupla
   (catálogo de regras + conferência independente), cofre local de credencial (WebCrypto),
   publicação via GitHub Contents API, exportação de imagem para WhatsApp, lembrete via WhatsApp.
   Criadas 12 notas com granularidade C4/Backstage (nunca uma nota genérica): 1 nó-projeto, 3
   capacidades específicas, 3 capacidades reutilizáveis, 2 pesquisas já auditadas (ponteiro, não
   cópia), 2 erros reais com correção, 1 decisão (esta implantação). **Ligação cross-projeto real,
   não hipotética:** confirmado por leitura do código (`docs/LEMBRETE_WHATSAPP.md`,
   `scripts/vps/lembrete_escala.py`) que este projeto já usa a MESMA VPS/Evolution API que o
   `charmway-erp` já cataloga como recurso compartilhável (`Infraestrutura-VPS-WhatsApp-CharmWay`)
   — link registrado nos dois sentidos. `checar-cofre.mjs`: 83/83 notas do Cofre inteiro íntegras
   (0 links quebrados, 0 órfãs, `fonte:arquivo:linha` presente em toda nota `capacidade`).
5. Cometidos, no processo de escrever as notas do Cofre, **2 casos reais do ERRO 41** (citar
   sintaxe de link literalmente em prosa) — pegos pelo próprio portão do Cofre no instante da
   edição e corrigidos antes de seguir. Prova viva de que o mecanismo funciona.
6. **Achado rodando o pré-voo completo (Passo 4), fora do escopo original mas corrigido no
   caminho:** `scripts/experimento-busca-local-em-escala.mjs` (criado em 20/08, S-068) nunca tinha
   sido registrado em `docs/capacidades.json` — a árvore de capacidades o via como órfão. Corrigido.
7. Cron noturno de fechamento (Passo 6): `escala-porteiros` **já está** na lista que o Flavio citou
   no próprio prompt — nada a pedir.

**Status:** 🔶 em andamento — Cofre e Diário implantados e verificados; falta rodar o pré-voo
completo até o fim e escrever o relatório final (Passo 4.3), e o Flavio ainda não confirmou os dois
achados fora do escopo original que ficaram de fora por decisão consciente (autor git divergente do
declarado em `docs/pre-voo.json`; `.env.local` ausente).

**Aprendizado:** a granularidade do Cofre só sai certa lendo código de verdade, linha por linha —
tentar preencher `capacidade`/`fonte` de memória (mesmo sendo o mesmo assistente que escreveu o
código, na mesma sessão) teria citado linha errada pelo menos duas vezes (o número de `gerar()` em
`gerador.ts` e o de `conferirPorFora()` mudaram de posição desde que eu os toquei pela última vez
nesta sessão).

**Cuidado:** o portão do Cofre roda a CADA edição (hook `PostToolUse`) — inclusive dentro da
PRÓPRIA nota que descreve a regra de não citar sintaxe de link em prosa. Descrever o bug em palavras
(nunca colchete-parêntese nem colchete-duplo fora de crase) evita o retrabalho de escrever, ver o
portão reprovar, e corrigir — mesmo quando a intenção é só dar um EXEMPLO do que não fazer.

### 19:35 SP — Cofre de Conhecimento é destino obrigatório de todo trabalho consequente, no mesmo passo

**Pedido:** o Flavio reforçou, com todas as letras, que o Cofre (regra global #11) não é retrato
ocasional — é destino obrigatório de TODO trabalho consequente, em TODO projeto, com três exigências
juntas: documental (nota real com frontmatter completo, não menção solta), ampla (cobertura completa
do que foi discutido/criado, em todos os projetos) e auditada pelo portão (`checar-cofre.mjs` real,
não "eu digo que documentei") — no mesmo passo do trabalho, não depois.

**Feito:** a lacuna era real — a correção do autor git e o endurecimento do
`diario_solicitacoes.bloqueia` (entrada anterior, 18:52) já estavam commitados e publicados, mas sem
retroalimentar o Cofre. Fechado agora: 3 notas `erro` novas (skill `portabilidade-handoff` versionada
mas nunca instalada; `diario_solicitacoes.bloqueia` declarado mas nunca lido pelo `registrar()`,
achado ao ler o código pra responder o Flavio com precisão; `Date.UTC`+`.toISOString` reprovado pelo
`portao-datas.mjs` mesmo internamente consistente) e 1 nota `decisao` consolidando as duas decisões
do dono, todas ligadas em `cofre/INDICE.md`. Verificado com `node
D:\Antigravity\_padroes-globais\scripts\checar-cofre.mjs`: 91/91 notas alcançáveis, 0 link quebrado,
0 nota órfã. Commit `padroes-globais@938d513`, empurrado. Também salvei isto como memória permanente
(`feedback_cofre_retroalimentacao_obrigatoria.md`) para não repetir a lacuna em sessão futura.

**Status:** ✅ feito — as 4 notas passam no portão; a lacuna deste dia está fechada. Backfill de
histórico ANTERIOR a hoje (inclui o nó próprio do `escala-geral` no Cofre) continua pendência
separada e já registrada, por desenho (`COFRE_DE_CONHECIMENTO.md` §7) — não é o mesmo tipo de lacuna
que o Flavio apontou agora.

**Aprendizado:** a lacuna não era falta de autonomia (eu já executo sem pedir permissão) — era um
passo do checklist do padrão-ouro (REGISTRAR → Cofre) sendo tratado como opcional na prática, mesmo
com a regra escrita dizendo "no mesmo passo". Exatamente o padrão "regra escrita e inerte" que as 3
notas de erro catalogadas nesta mesma entrada descrevem noutro contexto.

**Cuidado:** o Cofre é um arquivo compartilhado entre sessões concorrentes (confirmado ao vivo: outra
sessão editava `cofre/INDICE.md` e criando notas `erro` próprias ao mesmo tempo desta). Reler o
arquivo real antes de editar, e commitar só os arquivos genuinamente meus — nunca assumir que o
relatório do hook reflete só a própria edição quando há sinal de nota alheia (nome que não escrevi)
aparecendo no relatório.
